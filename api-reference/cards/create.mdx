---
title: 'Create Card'
openapi: 'POST /cards'
---

{/* <Snippet file="authorization.mdx" /> */}

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/cards \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "customerId": "user",
    "lastname": "user",
    "middlename": "user",
    "country": "user",
    "email": "user@mailinator.com"
  }'
```

```javascript Node.js
import { SwyftpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwyftpayClient(config);

await swyftpay.card.create({
    customerId: "user",
    lastname: "user",
    middlename: "user",
    country: "user",
    email: "user@mailinator.com"
})
```

```php PHP
use SwyftpayClient\SDK\Client;

$swyftpayClient = new Client(<SECRET_KEY>, <BUSINESS_ID>);

$swyftpayClient->card->create([
    "customerId" => "user",
    "lastname" => "user",
    "middlename" => "user",
    "country" => "user",
    "email" => "user@mailinator.com"
])->toArray();
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


    data := swyftpayClient.CardCreate{
        CustomerId: "user",
        Lastname: "user",
        Middlename: "user",
        Country: "user",
        Email: "user@mailinator.com"
    }

	resp, err := swyftpayClient.card.create(ctx, data)

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>

{/* <ResponseExample>

```json Response
{
    "data": {
        "acknowledged": true,
        "status": "processed",
        "transactionId": "string"
    }
}
```

</ResponseExample> */}
