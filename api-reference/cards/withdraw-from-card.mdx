---
title: 'Withdraw from Card'
openapi: 'POST /cards/{id}/withdraw'
---

{/* <Snippet file="authorization.mdx" /> */}

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/cards/{id}/withdraw \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 1000
  }'
```

```javascript Node.js
import { SwyftpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwyftpayClient(config);

await swyftpay.card.withdraw({
    cardId: "user",
    amount: "user"
})
```

```php PHP
use SwyftpayClient\SDK\Client;

$swyftpayClient = new Client(<SECRET_KEY>, <BUSINESS_ID>);

$swyftpayClient->card->withdraw([
    "cardId" => "user",
    "amount" => "user"
])->toArray();
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


    data := swyftpayClient.CardWithdraw{
        CardId: "user",
        Amount: 1000,
    }

	resp, err := swyftpayClient.card.withdraw(ctx, data)

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>

{/* <ResponseExample>

```json Response
{
    "data": {
        "acknowledged": true,
        "status": "processed",
        "transactionId": "string"
    }
}
```

</ResponseExample> */}
