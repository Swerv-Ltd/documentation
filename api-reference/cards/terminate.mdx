---
title: 'Terminate Card'
openapi: 'POST /cards/{id}/terminate'
---

<RequestExample>

```bash cURL
curl -X PUT https://api.swyftpay.io/api/v1/cards/{id}/terminate \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{

  }'
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.card.terminate('<CARD_ID>')
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$swyftpayClient = new Swervpay($config);

$swyftpayClient->card->terminate('<CARD_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })

	resp, err := swyftpayClient.card.terminate(ctx, '<CARD_ID>')

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>

