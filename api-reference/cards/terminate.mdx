---
title: 'Terminate Card'
openapi: 'POST /cards/{id}/terminate'
---

<RequestExample>

```bash cURL
curl -X PUT https://api.swyftpay.io/api/v1/cards/{id}/terminate \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{

  }'
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.card.terminate('<CARD_ID>')
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$client = new Swervpay($config);

$client->card->terminate('<CARD_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swervpay "github.com/swerv-ltd/swervpay-go"
	"log"
)

func main() {
	ctx := context.Background()

	client := swervpay.NewSwervpayClient(&swervpay.SwervpayClientOption{
        BusinessId: "<BUSINESS_ID>",
        SecretKey: "<SECRET_KEY>",
    })

	resp, err := client.Card.Terminate(ctx, "<CARD_ID>")

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>

