---
title: 'Get Wallet'
openapi: 'GET /wallets/{id}'
---

{/* <Snippet file="authorization.mdx" /> */}
<RequestExample>

```bash cURL
curl -X GET https://api.swyftpay.io/api/v1/wallets/{id} \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json"
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.wallet.get('<WALLET_ID>')
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$swyftpayClient = new Swervpay($config);

$swyftpayClient->wallet->get('<WALLET_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


	resp, err := swyftpayClient.wallet.get(ctx, '<WALLET_ID>')

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
