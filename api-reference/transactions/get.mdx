---
title: 'Get Transactions'
openapi: 'GET /transactions/{id}'
---

{/* <Snippet file="authorization.mdx" /> */}
<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/transactions/{id} \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json"
```

```javascript Node.js
import { SwyftpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwyftpayClient(config);

await swyftpay.transaction.get('{id}')
```

```php PHP
use SwyftpayClient\SDK\Client;

$swyftpayClient = new Client(<SECRET_KEY>, <BUSINESS_ID>);

$swyftpayClient->transaction->get('{id}')->toArray();
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })

	resp, err := swyftpayClient.transaction.get(ctx, '{id}')

	if err != nil {
		log.Fatal("novu error", err.Error())
		return
	}
}
```

</RequestExample>
