---
title: 'Test Webhook'
openapi: 'POST /webhook/{id}/test'
---

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/webhook/{id}/test \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json"
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.webhook.test('<WEBHOOK_ID>');
```


```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$client = new Swervpay($config);

$client->webhook->test('<WEBHOOK_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swervpay "github.com/swerv-ltd/swervpay-go"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	client := swervpay.NewSwervpayClient(&swervpay.SwervpayClientOption{
        BusinessId: businessId
        SecretKey: secretKey
    })

	resp, err := client.Webhook.Test(ctx, "<WEBHOOK_ID>")

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
