---
title: 'Get Customer'
openapi: 'GET /customers/{id}'
---

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/customers/{id} \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json"
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.customer.get('{id}')
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$client = new Swervpay($config);

$client->customer->get('<CUSTOMER_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swervpay "github.com/swerv-ltd/swervpay-go"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	client := swervpay.NewSwervpayClient(&swervpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


	resp, err := client.Customer.get(ctx, '{id}')

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
