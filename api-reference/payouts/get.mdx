---
title: 'Get payout'
openapi: 'GET /payouts/{id}'
---

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/payouts/{id} \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json"
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.payout.get('{id}')
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$swyftpayClient = new Swervpay($config);

$swyftpayClient->payout->get('<PAYOUT_ID>');
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


	resp, err := swyftpayClient.payout.get(ctx, '<PAYOUT_ID>')

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
