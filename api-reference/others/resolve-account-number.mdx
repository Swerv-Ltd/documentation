---
title: 'Resolve Account Number'
openapi: 'POST /resolve-account-number'
---

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/resolve-account-number \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
    -d '{
        "account_number": "user",
        "bank_code": "user"
    }'
```

```javascript Node.js
import { SwervpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwervpayClient(config);

await swyftpay.other.resolve_account_number({
    'accountNumber': 'user',
    'bankCode': 'user'
})
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$client = new Swervpay($config);

$client->other->resolve_account_number([
    'account_number' => 'user',
    'bank_code' => 'user
]);
```


```go Go
package main

import (
	"context"
	"fmt"
	swervpay "github.com/swerv-ltd/swervpay-go"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	client := swervpay.NewSwervpayClient(&swervpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


    data := swervpay.ResolveAccountNumber{
        AccountNumber: "user",
        BankCode: "user"
    }


	resp, err := client.Other.resolveAccountNumber(ctx, data)

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
