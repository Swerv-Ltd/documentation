---
title: 'Resolve Account Number'
openapi: 'POST /resolve-account-number'
---

<RequestExample>

```bash cURL
curl -X POST https://api.swyftpay.io/api/v1/resolve-account-number \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
    -d '{
        "account_number": "user",
        "bank_code": "user"
    }'
```

```javascript Node.js
import { SwyftpayClient } from '@swervpaydev/sdk';

const config = {
    secretKey: "<SECRET_KEY>",
    businessId: "<BUSINESS_ID>"
}

const swyftpay = new SwyftpayClient(config);

await swyftpay.other.resolveAccountNumber({
    'accountNumber': 'user',
    'bankCode': 'user'
})
```

```php PHP
use Swervpaydev\SDK\Swervpay;

$config = [
	'business_id' => '<BUSINESS_ID>',
	'secret_key' => '<SECRET_KEY>'
];

$swyftpayClient = new Swervpay($config);

$swyftpayClient->other->resolve_account_number([
    'account_number' => 'user',
    'bank_code' => 'user
]);
```


```go Go
package main

import (
	"context"
	"fmt"
	swyftpay "github.com/Swerv-Ltd/swyftpay-go/lib"
	"log"
)

func main() {
	secretKey := "<SECRET_KEY>"
    businessId := "<BUSINESS_ID>"
	ctx := context.Background()

	swyftpayClient := swyftpay.NewAClient(&swyftpay.Config{
        BusinessId: businessId
        SecretKey: secretKey
    })


    data := swyftpayClient.ResolveAccountNumber{
        AccountNumber: "user",
        BankCode: "user"
    }


	resp, err := swyftpayClient.other.resolveAccountNumber(ctx, data)

	if err != nil {
		log.Fatal("error", err.Error())
		return
	}
}
```

</RequestExample>
